#include <bits/stdc++.h>

using namespace std;

struct Box{
    int l, w, h;
}

int main() {
    std::ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n;
    cin >> n;
    vector<vector<Box>> boxes;
    for (int i = 0; i < n; ++i) {
        int l, w, h;
        cin >> l >> w >> h;
        boxes.push_back()
    }
}